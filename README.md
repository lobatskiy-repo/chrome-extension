# ThreeKit Modular Parser

Модульне розширення Chrome для парсингу та декодування URL-адрес ThreeKit з можливістю розширення функціоналу.

## 📋 Опис

ThreeKit Modular Parser — це розширення для Chrome, яке дозволяє легко аналізувати та декодувати складні URL-адреси ThreeKit. Розширення має модульну архітектуру, що дозволяє легко додавати нові способи обробки даних.

### Основні можливості:

- 🔍 **Full JSON Decode** — декодує всі URL параметри та вкладений JSON
- 🆔 **Asset IDs List** — витягує ID ресурсів з конфігурації
- 📥 **Швидке захоплення URL** — автоматично бере URL з поточної вкладки
- 🧩 **Модульна архітектура** — легко додавати нові модулі обробки

## 🚀 Швидкий старт

### Встановлення

1. Клонуйте репозиторій:
```bash
git clone https://github.com/your-username/threekit-parser-extension.git
cd threekit-parser-extension
```

2. Завантажте розширення в Chrome:
   - Відкрийте `chrome://extensions/`
   - Увімкніть "Режим розробника" (Developer mode)
   - Натисніть "Завантажити розпаковане" (Load unpacked)
   - Виберіть папку проекту

### Використання

1. Клікніть на іконку розширення в панелі Chrome
2. Натисніть кнопку "📥 Взяти URL з поточної вкладки" або вставте URL вручну
3. Натисніть "🚀 Розпарсити"
4. Переглядайте результати роботи кожного модуля

## 📁 Структура проекту

```
threekit-parser-extension/
├── manifest.json              # Конфігурація розширення Chrome
├── popup.html                 # HTML інтерфейс popup
├── style.css                  # Стилі інтерфейсу
├── build-scripts/
│   └── build_extension.py     # Скрипт для створення структури проекту
└── src/
    ├── popup.js               # Головний JavaScript файл
    ├── core/
    │   └── ModuleManager.js   # Менеджер модулів
    └── modules/
        ├── baseDecoder.js     # Модуль декодування JSON
        └── idsExtractor.js    # Модуль витягування ID
```

## 🧩 Модульна система

### Як працює ModuleManager

`ModuleManager` — це ядро системи, яке керує всіма модулями обробки URL.

**Основні методи:**
- `register(module)` — реєструє новий модуль
- `runAll(urlString)` — запускає всі зареєстровані модулі

### Вбудовані модулі

#### 1. Base Decoder (`baseDecoder.js`)
Декодує всі параметри URL та намагається розпарсити JSON:
```javascript
{
  host: "preview.threekit.com",
  path: "/...",
  params: {
    configuration: { ... },
    // інші параметри
  }
}
```

#### 2. IDs Extractor (`idsExtractor.js`)
Витягує тільки Asset IDs з configuration:
```javascript
{
  "attributeName": "asset-id-12345",
  "Stage Config": { ... }
}
```

## 🔧 Додавання власного модуля

1. Створіть новий файл у папці `src/modules/`:
```javascript
// src/modules/myCustomModule.js
export const myCustomModule = {
    name: "My Custom Parser",
    description: "Описання того, що робить модуль",
    run: (urlString, urlObject) => {
        // Ваша логіка обробки
        return {
            result: "parsed data"
        };
    }
};
```

2. Зареєструйте модуль у [src/popup.js](src/popup.js#L1):
```javascript
import { myCustomModule } from './modules/myCustomModule.js';

// ...
manager.register(baseDecoder);
manager.register(idsExtractor);
manager.register(myCustomModule); // Додайте свій модуль
```

### Вимоги до модуля:
- Експортувати об'єкт з полями `name` та `run`
- Функція `run` повинна приймати `(urlString, urlObject)`
- Функція `run` повинна повертати об'єкт або примітив для відображення

## 🛠️ Розробка

### Автоматична збірка структури

Проект включає Python скрипт для автоматичного створення базової структури:

```bash
cd build-scripts
python build_extension.py
```

Цей скрипт створює всю необхідну структуру папок та файлів проекту.

### Налагодження

1. Відкрийте DevTools для popup: правий клік на іконці → "Inspect popup"
2. Переглядайте консоль для помилок та логів
3. Змінюйте код та перезавантажуйте розширення (`chrome://extensions/` → кнопка оновлення)

## 📝 Технології

- **Chrome Extensions Manifest V3**
- **ES6 Modules**
- **Vanilla JavaScript** (без фреймворків)
- **CSS3** для сучасного UI

## 🤝 Внесок

Вітаються pull requests! Для значних змін спочатку відкрийте issue для обговорення.

### Що можна покращити:
- Додати більше модулів парсингу
- Додати експорт результатів (JSON, CSV)
- Додати історію парсингу
- Покращити UI/UX
- Додати тести

## 📄 Ліцензія

MIT

## 👤 Автор

Створено для спрощення роботи з URL-адресами ThreeKit.

---

**Версія:** 1.0  
**Підтримка:** Chrome 88+
